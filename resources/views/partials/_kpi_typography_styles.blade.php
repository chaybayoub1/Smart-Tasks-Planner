.kpi-label {
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: .06em;
    text-transform: uppercase;
    color: var(--kpi-muted);
    margin: 0 0 4px;
}
.kpi-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--kpi-text);
    margin: 0;
    line-height: 1;
    letter-spacing: -.02em;
}
.kpi-value small {
    font-size: 80%;
    font-weight: inherit;
    line-height: 0;
    letter-spacing: inherit;
}
.kpi-sub {
    font-size: .72rem;
    color: var(--kpi-sub-muted);
    margin: 4px 0 0;
}
